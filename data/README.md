# Data

- `sample/` — tiny, synthetic sample data safe to commit, used by labs/tests.
- `raw/`, `expected/` — git-ignored working dirs (see root `.gitignore`); never commit real or large data into the repo.

All data here is synthetic. Never commit real customer data, PII, or large datasets.
